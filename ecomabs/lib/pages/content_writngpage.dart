import 'package:flutter/material.dart';

class writing extends StatelessWidget {
  const writing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("content writing page"),
      ),
    );
  }
}
