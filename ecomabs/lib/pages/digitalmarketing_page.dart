import 'package:flutter/material.dart';

class digital extends StatelessWidget {
  const digital({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DIGITAL MARKETING (ITS)"),
      ),
    );
  }
}
