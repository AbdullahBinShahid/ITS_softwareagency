import 'package:flutter/material.dart';

class APP extends StatelessWidget {
  const APP({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("APP BANWALO PLEASE",),
      backgroundColor: Colors.green,
    
    )
    );
  }
}
